import{createClient}from'@supabase/supabase-js';import{readPublicEnv}from'./env.js';const env=readPublicEnv();export const isCloudConfigured=env.configured;export const supabase=env.configured?createClient(env.url,env.anonKey,{auth:{persistSession:true,autoRefreshToken:true,detectSessionInUrl:true},realtime:{params:{eventsPerSecond:4}}}):null;export function requireSupabase(){if(!supabase)throw new Error('Cloud sync is not configured.');return supabase}
