import React from'react';export function TimerConflictDialog({session,onResume,onKeep}){return <div className="overlay"><section className="import-dialog" role="dialog" aria-modal="true" aria-labelledby="conflict-title"><span className="eyebrow">ACTIVE SESSION</span><h2 id="conflict-title">Session already running</h2><p>A session started on another device at {new Date(session.started_at).toLocaleTimeString([],{hour:'2-digit',minute:'2-digit'})}.</p><div className="dialog-actions"><button onClick={onKeep}>Keep running there</button><button className="primary" onClick={()=>onResume(session)}>Resume here</button></div></section></div>}
