export function nextRevision(currentIndex,intervals=[1,3,7,14,30],from=new Date()){if(!Array.isArray(intervals)||!intervals.length||intervals.some(x=>!Number.isInteger(x)||x<1))throw new Error('Revision intervals must be positive days');const index=Math.min(currentIndex+1,intervals.length-1);return{intervalIndex:index,dueAt:new Date(+new Date(from)+intervals[index]*86400000).toISOString()}}
