export function validateRecurrence(rule){if(!rule)return null;if(!['daily','weekdays','weekly','custom'].includes(rule.type))throw new Error('Invalid recurrence type');if(rule.type==='custom'&&(!Array.isArray(rule.weekdays)||!rule.weekdays.length||rule.weekdays.some(x=>!Number.isInteger(x)||x<0||x>6)))throw new Error('Custom recurrence requires weekdays 0–6');return rule}
export function occursOn(rule,date){validateRecurrence(rule);if(!rule)return false;const day=new Date(date).getDay();if(rule.type==='daily')return true;if(rule.type==='weekdays')return day>0&&day<6;if(rule.type==='weekly')return day===new Date(rule.anchor||date).getDay();return rule.weekdays.includes(day)}
export function nextRecurrenceDue(rule,fromDate=Date.now()){validateRecurrence(rule);if(!rule)return null;const start=new Date(fromDate);for(let offset=1;offset<=14;offset++){const candidate=new Date(start);candidate.setDate(candidate.getDate()+offset);if(occursOn(rule,candidate))return+candidate}return+new Date(start.getTime()+86400000)}
