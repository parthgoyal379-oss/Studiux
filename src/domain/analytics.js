export function median(values){if(!values.length)return 0;const sorted=[...values].sort((a,b)=>a-b),mid=Math.floor(sorted.length/2);return sorted.length%2?sorted[mid]:(sorted[mid-1]+sorted[mid])/2}
export function analyticsSummary(sessions,tasks=[]){const complete=sessions.filter(x=>x.status==='COMPLETED'||x.endedAt||x.finished_at);const durations=complete.map(x=>x.duration||x.duration_seconds*1000||0);return{focusedMs:durations.reduce((a,x)=>a+x,0),sessionCount:complete.length,medianSessionMs:median(durations),taskCompletion:tasks.length?tasks.filter(x=>x.done||x.status==='DONE').length/tasks.length:null}}
